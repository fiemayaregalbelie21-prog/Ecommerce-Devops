{
  "name": "ecommerce-backend",
  "version": "1.0.0",
  "description": "Companion Node.js backend for the Flutter FakeStoreAPI e-commerce app. Handles order history and wishlist sync backed by PostgreSQL.",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test test/"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
