# wishlist feature

This feature manages user favorites and saved products.

## What it contains

- wishlist UI
- actions to add or remove items
- state for persisted favorite products

## Logic

The wishlist feature allows users to save products for later. It keeps track of which items the user has marked as favorites and updates that list when actions are performed.
