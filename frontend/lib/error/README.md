# error

This folder contains shared error handling utilities for the app.

## Purpose

It centralizes the logic for formatting and displaying app errors so that different screens can handle failures consistently.

## Why it matters

Instead of repeating error handling in every screen, the app uses a shared helper to present clear messages, retries, and fallback states.
